{{#each songs as |song|}}

  <!-- {{> partial}} -->
    <div data-id="{{song.id}}" class='row song'>
      <div class='col-lg-4'>
        {{song.name}}
      </div>
      <div class='col-lg-4'>
          {{song.artist}}
      </div>
      <div class='col-lg-3'>
          {{song.runtime}}
      </div>
    </div>
{{/each}}
